{
  "name": "ihtier-blog",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build:wasm": "bash wasm-apps/build.sh",
    "build": "node scripts/prebuild.mjs && npm run build:wasm && astro build && npx pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.5.0",
    "@astrojs/mdx": "^4.2.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
